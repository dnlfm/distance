# Example environment variables for local development
# Copy this file to .env and adjust values as needed. Do NOT commit .env with secrets.

# Nominatim service configuration
# When running locally with docker-compose, set NOMINATIM_URL to the nominatim container address
# (the compose file exposes it at http://nominatim:8080). If you want to use the public service,
# set RUN_LOCAL=false and leave NOMINATIM_URL empty or set to the public URL.
NOMINATIM_URL=http://nominatim:8080
PUBLIC_NOMINATIM_URL=https://nominatim.openstreetmap.org
USER_AGENT=distance-finder/1.0
RUN_LOCAL=true

# Database configuration (used by the app)
# Example: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://nominatim:nominatim@postgres:5432/nominatim

# Postgres container credentials (used by docker-compose for the postgres service)
POSTGRES_USER=nominatim
POSTGRES_PASSWORD=nominatim
POSTGRES_DB=nominatim

# Optional nominatim DB configuration for the nominatim container
NOMINATIM_DB_HOST=postgres
NOMINATIM_DB_USER=nominatim
NOMINATIM_DB_PASSWORD=nominatim
NOMINATIM_DB_NAME=nominatim

# Logging / misc
LOG_LEVEL=INFO

DOCKER_PLATFORM=linux/amd64